# TODO implement that - copy files & dirs from one directory to another maybe with direct refs or use a spezific folder for that
# maybe a structure like download-plugins
# Maybe sh scripts should also be split into scripts which need to be run in the container and which are outside